{
  "name": "critical-serena-refactoring-scripts",
  "version": "1.0.0",
  "description": "Performance baseline measurement scripts for critical-serena-refactoring",
  "main": "collect-metrics.js",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "build": "tsc",
    "collect": "ts-node collect-metrics.ts",
    "measure": "npm run build && node dist/collect-metrics.js"
  },
  "keywords": [
    "performance",
    "metrics",
    "baseline",
    "spring-boot",
    "actuator"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vitest/coverage-v8": "^1.0.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  }
}